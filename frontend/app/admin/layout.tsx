export default function AdminLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <div>
      {/* TODO: implement admin layout with dedicated sidebar */}
      {children}
    </div>
  )
}
