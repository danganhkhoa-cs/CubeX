export default function DashboardLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <div>
      {/* TODO: implement dashboard layout (navbar, footer, sidebar) */}
      {children}
    </div>
  )
}
